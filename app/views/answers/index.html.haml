.page-header
  %h1 Recents Questions and Answers

- @answers.each do |answer|
  .panel.panel-warning
    .panel-heading
      %h4
        Question by 
        = link_to answer.question.user, user_path(answer.question.user)
      %p
        = "#{truncate(answer.question.body, length: 200)}?"
        = link_to 'Answer', answer.question
    
    .panel-body
      %h4
        Answer by 
        = link_to answer.user, user_path(answer.user)
      %p
        = answer.body
        = link_to 'Reply', answer

%hr 
= will_paginate @answers, renderer: BootstrapPagination::Rails